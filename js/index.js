// Prevent us from attempting to use variables
// that are not declared
"use strict"
// 1.
/*function add(a, b) {
    const sum = a + b;
    console.log(sum);
    return sum;
}

add(3, 5);

function multiply(a, b) {
    const product = a * b;
    console.log(product);
    return product;
}

multiply(4, 7);

function concatenate(str1, str2) {
    const result = str1 + str2;
    console.log(result);
    return result;
}

concatenate('Hello', ' World'); */

//2.

/*function addNumbers(a, b) {
    return a + b;
}
let result = addNumbers(3, 5);
console.log(result);


function multiplyNumbers(a, b) {
    return a * b;
}
let result = multiplyNumbers(4, 6);
console.log(result); 


function greet(name) {
    console.log(`Hello, ${name}!`);
}
greet("Dale");

function repeatMessage(message, times) {
    for (let i = 0; i < times; i++) {
        console.log(message);
    }
}
repeatMessage("Today, is a good day!", 3);*/

//3.
/*function sumNumbers(a, b = 0, c = 0) {
    return a + b + c;
}

console.log(sumNumbers(10)); 
console.log(sumNumbers(10, 5));  

function applyDiscount(price, discount = 0.1, tax = 0.05) {
    const discountedPrice = price * (1 - discount);
    const finalPrice = discountedPrice * (1 + tax);
    return finalPrice;
}

console.log(applyDiscount(100));  // Default discount and tax applied
console.log(applyDiscount(100, 0.2));  // Custom discount, default tax
console.log(applyDiscount(100, 0.1, 0.07));  // Default discount, custom tax
*/



